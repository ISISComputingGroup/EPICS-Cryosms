ReplyTimeout = 200;
OutTerminator = CR LF;
InTerminator = 0x13;

getOutputMode {
    out "T";
    wait 50;
    in "%*8c UNITS: %{AMPS|TESLA}";
    wait 100;
}

setOutputMode {
    out "T %{OFF|ON}";
    wait 50;
    in "%*8c UNITS: %*{AMPS|TESLA}";
    wait 100;
}
